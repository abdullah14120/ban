<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- تعريف الصلاحية الخاصة والمحمية بالتوقيع لضمان عدم اختراق الاتصال -->
    <permission 
        android:name="com.abdullah.permission.SECURE_UPDATE" 
        android:protectionLevel="signature" />

    <!-- الصلاحيات الآمنة والمسموح بها قياسياً -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- الوصول للتخزين بما يتوافق مع سياسات Scoped Storage الحديثة -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="المساعد التقني الذكي" 
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity 
            android:name=".SecondActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity 
            android:name=".VerifyActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- إعداد FileProvider لمشاركة مسار ملف ZIP بأمان مع التطبيق الهدف -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.abdullah.assistant.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
    
</manifest>
